import { FiltroPeliculasPipe } from './filtro-peliculas.pipe';

describe('FiltroPeliculasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPeliculasPipe();
    expect(pipe).toBeTruthy();
  });
});
